<mah:MetroWindow x:Class="MovieFinder2025.Views.TrailerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconpacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:Dialog ="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:cefsharp="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        xmlns:local="clr-namespace:MovieFinder2025.Views"
        mc:Ignorable="d"
        Title="Youtube TrailerView" Height="560" Width="800"
        Dialog:DialogParticipation.Register="{Binding}"
        WindowStartupLocation="CenterScreen">
    <mah:MetroWindow.IconTemplate>
        <DataTemplate>
            <iconpacks:PackIconSimpleIcons Kind="Youtube" Margin="5,5,10,5" VerticalAlignment="Center" />
        </DataTemplate>
    </mah:MetroWindow.IconTemplate>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="510"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!--ui구성-->
        <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Content="{Binding MovieTitle}" FontSize="20" Margin="5" FontWeight="Bold" VerticalAlignment="Center"/>
        <ListView Grid.Row="1" Grid.Column="0" Margin="5" ItemsSource="{Binding YoutubeItems}" SelectedItem="{Binding SelectedYoutube}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command ="{Binding TrailerDoubleClickCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="썸네일" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Stretch="Fill" Source="{Binding Thumbanil}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="타이틀" Width="auto" DisplayMemberBinding="{Binding Title}"></GridViewColumn>
                    <GridViewColumn Header="채널명" Width="auto" DisplayMemberBinding="{Binding ChannelTitle}"></GridViewColumn>
                    <GridViewColumn Header="링크" Width="auto" DisplayMemberBinding="{Binding URL}"></GridViewColumn>

                </GridView>
            </ListView.View>
        </ListView>
        <Grid Grid.Row="1" Grid.Column="1"  Margin="10">
            <cefsharp:ChromiumWebBrowser Address="{Binding YoutubeUri}"/>
        </Grid>
    </Grid>
</mah:MetroWindow>
